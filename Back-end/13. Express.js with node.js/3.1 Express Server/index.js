import express from "express";
const app = express();
const port = 3000;

// app.listen(3000, () => {
//     console.log("server running on port 3000");
// });

app.listen(port,room);

function room (){
    console.log(`the server running on the ${port}.`);
    
}